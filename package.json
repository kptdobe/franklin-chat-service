{
  "name": "franklin-chat-microservice",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.12.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
